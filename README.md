# Solana-Network-.-HTML-
working on a self building, real-time network .....simple concept - the network expands due to users/builders activity on the network. Every idea, every interaction, every project adds to expanding the network all in real-time. It isn't just a network built for the users but is also built by the users.
